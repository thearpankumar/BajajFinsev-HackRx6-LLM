# ==============================================================================
# Minimal Production Requirements for BajajFinsev RAG System
# Use with pip-compile: pip-compile requirements.minimal.in
# ==============================================================================

# Core FastAPI and ASGI server
fastapi
uvicorn[standard]

# HTTP client for external API calls
httpx

# AI/ML Core (essential only)
openai
google-generativeai

# Vector Database (lightweight)
lancedb

# Text Processing (minimal - only NLTK)
nltk

# Document Processing (essential)
PyMuPDF
python-docx

# Search (lightweight BM25)
rank-bm25

# Core utilities
numpy
python-multipart
pydantic
pydantic-settings
python-dotenv

# ==============================================================================
# REMOVED FOR SIZE OPTIMIZATION:
# 
# Heavy ML Libraries (saves ~2GB):
# - torch (PyTorch - ~1.5GB)
# - transformers (Hugging Face - ~500MB) 
# - sentence-transformers (includes torch - ~300MB)
# - spacy (Advanced NLP - ~200MB)
# - scikit-learn (ML algorithms - ~100MB)
# - pandas (Data processing - ~100MB)
# - scipy (Scientific computing - ~50MB)
# 
# Development/Evaluation Tools:
# - ragas (Evaluation framework)
# - langchain (Not needed for core functionality)
# - datasets (Only needed for training/evaluation)
# 
# Optional Dependencies:
# - psutil (System monitoring - can be added if needed)
# - ruff (Linting - only needed for development)
# 
# ==============================================================================
